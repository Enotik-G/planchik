services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: auth
      POSTGRES_USER: auth
      POSTGRES_PASSWORD: auf67
    ports:
      - "5432:5432"
    volumes:
      - auth:/var/lib/postgresql/data
